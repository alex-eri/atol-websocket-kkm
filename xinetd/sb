# если надо разделить фискальник на два рабочих места, используем xinetd в качестве прокси

service sb
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    wait = no
    redirect = 192.168.10.254 9113
    bind = 127.0.0.1
    port = 9113
    user = nobody
}
